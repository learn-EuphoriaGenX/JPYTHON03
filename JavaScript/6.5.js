let isAvailable = true;
let price = 80

if (isAvailable == 'Hello' && price <= 300) {
    console.log("Hello is Available And u can pch.");
}
else if (isAvailable && price <= 200) {
    console.log("Product is Available and price below 200");
} else {
    console.log("Product is Not Available and u gareeb");
}
